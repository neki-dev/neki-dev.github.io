import { PrintMeta } from '~types';

import image from './image.png';
import source from './source.psd?url';

export const printPurple: PrintMeta = {
  url: image,
  hex: '#7A589C',
  name: 'PURPLE',
  source,
  size: 10,
  offset: 12,
};
