import { PrintMeta } from '~types';

import image from './image.png';
import source from './source.psd?url';

export const printLightblue: PrintMeta = {
  url: image,
  hex: '#00B7CC',
  name: 'LIGHTBLUE',
  source,
  size: 16,
  offset: 12,
};
